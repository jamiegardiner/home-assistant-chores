complete:
  target:
    entity:
      integration: chores
      domain: sensor
      device_class: enum
  fields:
    completed_at:
      required: false
      selector:
        datetime:

snooze:
  target:
    entity:
      integration: chores
      domain: sensor
      device_class: enum
  fields:
    value:
      required: false
      selector:
        number:
          min: 1
          max: 365
          step: 1
          mode: box
    unit:
      required: false
      selector:
        select:
          options:
            - minutes
            - hours
            - days
            - weeks

snooze_exact:
  target:
    entity:
      integration: chores
      domain: sensor
      device_class: enum
  fields:
    snooze_until:
      required: true
      selector:
        datetime:

unsnooze:
  target:
    entity:
      integration: chores
      domain: sensor
      device_class: enum
