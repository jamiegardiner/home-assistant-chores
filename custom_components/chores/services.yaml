complete:
  target:
    device:
      integration: chores
    entity:
      integration: chores
      domain: sensor
      device_class: enum

snooze:
  target:
    device:
      integration: chores
    entity:
      integration: chores
      domain: sensor
      device_class: enum
  fields:
    snooze_days:
      required: false
      selector:
        number:
          min: 1
          step: 1
          mode: box
    snooze_weeks:
      required: false
      selector:
        number:
          min: 1
          step: 1
          mode: box
    snooze_until:
      required: false
      selector:
        date:

unsnooze:
  target:
    device:
      integration: chores
    entity:
      integration: chores
      domain: sensor
      device_class: enum
