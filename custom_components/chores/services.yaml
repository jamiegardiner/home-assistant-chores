complete:
  target:
    entity:
      integration: chores
      domain: sensor
      device_class: enum

snooze:
  target:
    entity:
      integration: chores
      domain: sensor
      device_class: enum
  fields:
    value:
      required: true
      selector:
        number:
          min: 1
          step: 1
          mode: box
    unit:
      required: true
      selector:
        select:
          options:
            - minutes
            - hours
            - days
            - weeks

unsnooze:
  target:
    entity:
      integration: chores
      domain: sensor
      device_class: enum
